% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_select_gene_and_transcript.R
\name{introns_jx_to_GRange}
\alias{introns_jx_to_GRange}
\title{Build intron-junction windows for selected transcripts}
\usage{
introns_jx_to_GRange(gene_tx, annotation, refseq_introns_exons, debug = FALSE)
}
\arguments{
\item{gene_tx}{\code{data.table} with transcript selection, including
\code{tx_version_id}.}

\item{annotation}{Sequence naming style used by aligned BAM files. If set to
\code{"UCSC"}, seqlevels are converted to UCSC style.}

\item{refseq_introns_exons}{\code{data.table} containing RefSeq exon/intron
annotations.}

\item{debug}{Optional output directory for writing intermediate TSV files.
Use \code{FALSE} (default) to disable debug output.}
}
\value{
A list with:
\itemize{
\item \code{starts}: \code{GRanges} centred on intron starts.
\item \code{ends}: \code{GRanges} centred on intron ends.
}
}
\description{
Internal helper that creates \code{GRanges} windows around intron starts and ends
(exon-intron boundaries) using \code{INTRON_JUNCTION_UPSTREAM} and
\code{INTRON_JUNCTION_DOWNSTREAM}.
}
\examples{
\dontrun{
gene_tx <- data.table::data.table(
  gene_name = "COL2A1",
  tx_version_id = "NM_001844.5"
)

# Example 1: junction windows for COL2A1 (UCSC style)
jx <- cortar:::introns_jx_to_GRange(gene_tx, "UCSC", refseq_introns_exons_hg38)
c(length(jx$starts), length(jx$ends))

# Example 2: same query with 1000 Genomes sequence style
jx2 <- cortar:::introns_jx_to_GRange(gene_tx, "1000genomes", refseq_introns_exons_hg38)
jx2$starts
}
}
\keyword{internal}
